---
title: "Random Forest Boostrap vs Subsampling"
output: html_notebook
---

```{r}
path <- '../../../Utils/'
source(paste0(path, 'Utils.R'))
```

```{r}
results.path <- '../Result/'
res <- computeResults(results.path)
plotResults(res$mean.error, res$difference.error, '../../../../Data/uci/processed/')
```

